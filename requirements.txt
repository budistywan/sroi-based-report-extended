# SROI Report Generation System — Python Dependencies
# Sprint 0–14E baseline (frozen @ 14E)
#
# Install:
#   pip install -r requirements.txt

# Core document generation
python-docx>=1.1.0

# JSON schema validation (Sprint 13)
jsonschema>=4.20.0

# No other external Python packages required.
# All other dependencies (re, json, pathlib, subprocess, etc.)
# are Python standard library.
